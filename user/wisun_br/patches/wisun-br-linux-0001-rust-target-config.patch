--- /dev/null
+++ b/cargo-target.config
@@ -0,0 +1,28 @@
+[target.mips-unknown-linux-musl]
+ar = "mips-linux-musl-20220202-ar"
+linker = "mips-linux-musl-20220202-gcc"
+rustflags = ["-C", "link-args=-EL -latomic"]
+
+[target.mipsel-unknown-linux-musl]
+ar = "mips-linux-musl-20220202-ar"
+linker = "mips-linux-musl-20220202-gcc"
+rustflags = ["-C", "link-args=-EL -latomic"]
+
+[target.arm-unknown-linux-musleabi]
+ar = "arm-linux-musleabi-20220202-ar"
+linker = "arm-linux-musleabi-20220202-gcc"
+rustflags = ["-C", "link-args=-lc -latomic", "-C", "target-feature=-crt-static"]
+
+[target.aarch64-unknown-linux-musl]
+ar = "aarch64-linux-musl-20220202-ar"
+linker = "aarch64-linux-musl-20220202-gcc"
+rustflags = ["-C", "link-args=-fstack-protector-strong -latomic", "-C", "target-feature=-crt-static"]
+
+[target.x86_64-unknown-linux-musl]
+ar = "x86_64-linux-musl-20220202-ar"
+linker = "x86_64-linux-musl-20220202-gcc"
+rustflags = ["-C", "link-args=-fomit-frame-pointer -fstack-protector-strong -latomic", "-C", "target-feature=-crt-static"]
+
+[target.x86_64-unknown-linux-gnu]
+rustflags = ["-C", "link-args=-lc -latomic", "-C", "target-feature=-crt-static"]
+
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -274,6 +274,10 @@
         set(RUST_BUILD_FLAG --release)
     endif()
 
+    if(DEFINED RUST_CARGO_EXTRA_FLAGS)
+        set(RUST_BUILD_FLAG ${RUST_BUILD_FLAG} ${RUST_CARGO_EXTRA_FLAGS})
+    endif()
+
     add_custom_target(cargo-wsbrd_cli ALL DEPENDS wsbrd_cli)
     add_custom_command(OUTPUT wsbrd_cli
         COMMAND ${CARGO_COMMAND} build ${RUST_BUILD_FLAG} --target=${RUST_TARGET} --target-dir=cargo/

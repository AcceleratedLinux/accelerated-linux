diff -Naur busybox-1.36.1.org/miscutils/rx.c busybox-1.36.1/miscutils/rx.c
--- busybox-1.36.1.org/miscutils/rx.c	2018-12-06 00:44:34.000000000 +1000
+++ busybox-1.36.1/miscutils/rx.c	2019-07-02 14:26:34.514572676 +1000
@@ -80,7 +80,6 @@
 	unsigned length = 0;
 	int do_crc = 1;
 	char reply_char;
-	unsigned timeout = TIMEOUT_LONG;
 
 	/* Flush pending input */
 	tcflush(read_fd, TCIFLUSH);
@@ -96,7 +95,7 @@
 		unsigned expected;
 		int i, j;
 
-		blockBegin = read_byte(timeout);
+		blockBegin = read_byte(TIMEOUT_LONG);
 		if (blockBegin < 0)
 			goto timeout;
 
@@ -123,7 +122,6 @@
 			goto fatal;
 		}
 
-		timeout = TIMEOUT;
 		reply_char = NAK;
 
 		switch (blockBegin) {

#!/bin/sh

#
# serial-mode.sh - setup and control serial port interface mode
#
# This control script is specific to the Digi/ConnectEZ1.
#
# (C) Copyright 2020, Digi International.
#

#
# GPIO576 = UART GPIO 4: differential termination enable (output, active high, default low)
# GPIO577 = UART GPIO 5: 2-wire, half-duplex enable (output, active high, default low)
# GPIO578 = UART GPIO 6: RS485 enable (output, active low, default high)
# GPIO579 = UART GPIO 7: RS232 enable (output, active high, default low)
#

serial_init() {
	# The initialization prepares GPIOs, but defers mode shift
	# until mode is selected.
	echo 576 > /sys/class/gpio/export
	echo 577 > /sys/class/gpio/export
	echo 578 > /sys/class/gpio/export
	echo 579 > /sys/class/gpio/export
}

serial_rs232() {
	echo low > /sys/class/gpio/gpio576/direction
	echo low > /sys/class/gpio/gpio577/direction
	echo high > /sys/class/gpio/gpio578/direction
	echo high > /sys/class/gpio/gpio579/direction
}

serial_rs422() {
	echo low > /sys/class/gpio/gpio576/direction
	echo low > /sys/class/gpio/gpio577/direction
	echo low > /sys/class/gpio/gpio578/direction
	echo low > /sys/class/gpio/gpio579/direction
}

serial_rs485() {  # 2-wire, half-duplex
	echo low > /sys/class/gpio/gpio576/direction
	echo high > /sys/class/gpio/gpio577/direction
	echo low > /sys/class/gpio/gpio578/direction
	echo low > /sys/class/gpio/gpio579/direction
}

serial_nonterminate() {
	echo low > /sys/class/gpio/gpio576/direction
}

serial_terminate() {
	echo high > /sys/class/gpio/gpio576/direction
}

usage() {
	echo "usage: serial-mode <device> (init|rs-232|rs-422|rs-485) [terminate]"
	echo
	echo "        init      -- initialize port control"
	echo "        powerdown -- not supported"
	echo "        rs-232    -- set port into RS-232 mode (default)"
	echo "        rs-422    -- set port into RS-422 mode"
	echo "        rs-485    -- set port into RS-485 mode"
	echo "        terminate -- enable line termination"
	echo
	exit 1
}

#
# Check arguments, and error out on anything invalid.
#
if [ $# -lt 2 -o $# -gt 3 ] ; then
	usage
fi
if [ "$1" != "/dev/serial/port1" ] ; then
	echo "ERROR: serial-mode only supported on /dev/serial/port1"
	exit 1
fi

case "$2" in
init)	serial_init ;;
powerdown)
	# Not supported, exit with success
	exit 0
	;;
rs-232)	serial_rs232 ;;
rs-422)	serial_rs422 ;;
rs-485)	serial_rs485 ;;
*)	usage ;;
esac

if [ "$3" = "terminate" ] && [ "$2" != "rs-232" ] ; then
	serial_terminate
else
	serial_nonterminate
fi

exit 0

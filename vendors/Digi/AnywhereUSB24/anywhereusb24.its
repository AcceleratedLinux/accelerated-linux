/dts-v1/;

/ {
	description = "U-Boot fitImage for Digi AnywhereUSB Plus";
	#address-cells = <1>;

	images {
		kernel@1 {
			description = "Linux kernel";
			data = /incbin/("kernel.gz");
			type = "kernel";
			arch = "arm64";
			os = "linux";
			compression = "gzip";
			load = <0x80080000>;
			entry = <0x80080000>;
			hash@1 {
				algo = "sha1";
			};
		};

		fdt@anywhereusb24.dtb {
			description = "Flattened Device Tree blob";
			data = /incbin/("anywhereusb24.dtb");
			type = "flat_dt";
			arch = "arm64";
			compression = "none";
			load = <0x90000000>;
			hash@1 {
				algo = "sha1";
			};
		};
	};

	configurations {
		default = "conf@anywhereusb24.dtb";
		conf@anywhereusb24.dtb {
			description = "1 Linux kernel, FDT blob";
			kernel = "kernel@1";
			fdt = "fdt@anywhereusb24.dtb";
			hash@1 {
				algo = "sha1";
			};
		};
	};
};

load_env -f /opt/boot/grubenv

if [ "$default" == "1" ]; then
  linux (image)/boot/vmlinuz bootpart=b console=ttyS0,115200 init=/bin/init-loop
  initrd /image1
else
  linux (image)/boot/vmlinuz bootpart=a console=ttyS0,115200 init=/bin/init-loop
  initrd /image
fi

boot

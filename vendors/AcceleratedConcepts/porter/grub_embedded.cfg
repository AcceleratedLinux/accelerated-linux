serial --unit=0 --speed=115200
terminal_output serial
terminal_input serial

load_env -f /opt/boot/grubenv

if [ "$default" == "1" ]; then
	loopback image /image1
else
	loopback image /image
fi

configfile (image)/boot/grub/grub.cfg
